{
  "name": "Wize Gateway (RTL-SDR)",
  "version": "1.0.0",
  "slug": "wize_gateway",
  "description": "Gateway Wize pour compteurs Suez via RTL-SDR",
  "arch": ["amd64", "armv7", "aarch64"],
  "startup": "services",
  "boot": "auto",
  "ports": {},
  "ports_description": {},
  "map": ["config:rw", "share:rw"],
  "options": {
    "frequency": 169475000,
    "gain": 38,
    "mqtt_host": "core-mosquitto",
    "mqtt_port": 1883,
    "mqtt_username": "",
    "mqtt_password": "",
    "mqtt_topic": "wize"
  },
  "schema": {
    "frequency": "int",
    "gain": "int",
    "mqtt_host": "str",
    "mqtt_port": "int",
    "mqtt_username": "str",
    "mqtt_password": "str",
    "mqtt_topic": "str"
  }
}

